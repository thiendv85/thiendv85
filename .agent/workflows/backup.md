---
description: Backup and checkpoint source code using Git & Vercel
---
1. Use the `git-checkpoint-manager` skill.
2. Ask the user for a summary of changes.
3. Update `backup_history.md`.
4. Run `git add .`, `git commit`, and `git push`.
